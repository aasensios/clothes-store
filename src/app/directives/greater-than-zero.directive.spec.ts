import { GreaterThanZeroDirective } from './greater-than-zero.directive';

describe('GreaterThanZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new GreaterThanZeroDirective();
    expect(directive).toBeTruthy();
  });
});
